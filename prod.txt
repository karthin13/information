VT-12159 PROD Task 1 - Connection and Integration with Tosca DEX
  PROD has DEX gateways: 
    config_FCP_prod.tosca.dex.gateway1/2.url - you need to validate connection.
    Need to check the following 
      1.DEX Token: Check if Jenkins can get DEX token from PROD URL. If token fails, nothing will work.
      2.Execution: Check if test execution can be enqueued - means you send request to DEX to run Tosca tests.
      3.Status Polling: After enqueue, Jenkins should keep polling status - running / passed / failed. Check polling logic works in PROD.
      4.ToscaFeeder Downtime Guard: In file applabs_ToscaFeeder_release.groovy there is a guard logic. If ToscaFeeder is down, job should not fail blindly - it should handle downtime. Test this guard triggers correctly in PROD.
      5.JSL Check: Confirm agile_JSL@main loads correct Tosca methods from JSLTosca.groovy. 
            Means shared library on main branch has correct code.

VT-12160. PROD Task 2 - Checking E2E Connection 
     1. Run Seed Job in PROD:Run Jenkinsfile_fcp in PROD, but only in low-traffic window (e.g., night) 
          - because PROD is live. Don't break PROD during day.
     2. Smoke Test 7 Scenarios - all must pass:
           seed - seed job creates all jobs?
           manual Tosca - manually trigger Tosca test
           first cron - scheduled run works?
           notification - mail/Teams comes?
           K8s permissions - can access K8s?  
           PR trigger - PR triggers job?
           rollback - if fails, rollback works?
     3. K8s Namespace Check:
        Namespace is adp-tools-appl-abs-prod-team
        Validate secret operations - can Jenkins read secrets from this namespace? Like passwords, tokens.
     4. Paris DR Config:
           Paris (ew3) is Disaster Recovery site.
           Currently commented out in activeUrls. You need to either confirm it is NOT needed now, or formally defer it (create a follow-up ticket / get approval to keep it commented).

VT-12161 PROD Task 3 - Observe for 1 Week (Hypercare) 
     If any pipeline fails in PROD,  must fix immediately. No delay.
    Track daily:
      Build success rate - e.g., 98% pass, 2% fail?
      Failure modes - Why it failed? Is it DEX down? K8s issue? Code issue?
      PROD config differences - check config_FCP_prod - is anything different vs DEV/INT? Document it.
    Sign-off report: After 1 week , we can provide the report , PROD is stable
    
